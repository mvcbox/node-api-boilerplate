'use strict';

const path = require('path');

module.exports = {
  config: path.normalize(__dirname + '/../config/config.js'),
  'migrations-path': path.normalize(__dirname + '/../../src/database/migrations')
}
